<form class="login-form">
  {{#each inputs}}
    {{{ LabeledInput label=this.label name=this.name onFocusout=this.onFocusout }}}
  {{/each}}
  {{#each buttons}}
    {{{ Button label=this.label classType=this.classType onClick=this.onClick }}}
  {{/each}}
</form>
