    <label class="input input__container">
        {{{ Input label=this.label name=this.name onBlur=this.onBlur }}}
        {{{ InputError ref="error" validateMessage=this.validateMessage }}}
    </label>
