<section class="login">
    <h2 class="login__header">Login</h2>
    {{#> FormAuth}}
        {{> Input label="Login" }}
        {{> Input label="Password" }}
        {{> Button label="Sign in" type="primary" page="list"}}
        {{> Button label="Sign up" type="disabled" }}
    {{/FormAuth}}
</section>
